# ═══════════════════════════════════════════════════════════════════════
# Пример файла .env для Production (VPS)
# Создайте файл .env на сервере с этим содержимым
# ═══════════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────────
# ОСНОВНЫЕ НАСТРОЙКИ
# ───────────────────────────────────────────────────────────────────────

# SECRET_KEY - секретный ключ Django (ОБЯЗАТЕЛЬНО!)
# Сгенерируйте командой:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=замените-на-реальный-секретный-ключ-из-генератора

# DEBUG - режим отладки (ВСЕГДА False для продакшена!)
DEBUG=False

# ALLOWED_HOSTS - разрешенные хосты (через запятую БЕЗ пробелов)
# Для домена лин-кор.рф:
ALLOWED_HOSTS=xn----otbhblio.xn--p1ai,лин-кор.рф,www.xn----otbhblio.xn--p1ai,5.129.251.41

# ───────────────────────────────────────────────────────────────────────
# БАЗА ДАННЫХ POSTGRESQL
# ───────────────────────────────────────────────────────────────────────

# DATABASE_URL - строка подключения (получите из setup_server.sh)
# Формат: postgresql://USER:PASSWORD@HOST:PORT/DB_NAME
DATABASE_URL=postgresql://django_user:пароль_из_setup_server@localhost:5432/engineering_site_db

# ───────────────────────────────────────────────────────────────────────
# БЕЗОПАСНОСТЬ HTTPS
# ───────────────────────────────────────────────────────────────────────

# SECURE_SSL_REDIRECT - перенаправление HTTP → HTTPS
# Сначала False, потом после установки SSL → True
SECURE_SSL_REDIRECT=False

# После выполнения setup_ssl.sh измените на:
# SECURE_SSL_REDIRECT=True

# ═══════════════════════════════════════════════════════════════════════
# ИНСТРУКЦИЯ ПО СОЗДАНИЮ .env НА СЕРВЕРЕ
# ═══════════════════════════════════════════════════════════════════════
#
# 1. Подключитесь к серверу: ssh django@5.129.251.41
# 2. Перейдите в проект: cd ~/projects/engineering_site
# 3. Создайте файл: nano .env
# 4. Скопируйте содержимое этого файла
# 5. Замените значения на реальные:
#    - SECRET_KEY (из генератора)
#    - DATABASE_URL (из вывода setup_server.sh)
# 6. Сохраните: Ctrl+X, Y, Enter
#
# ═══════════════════════════════════════════════════════════════════════

